---
phase: 04-venue-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/dialogs/wizard/WizardView.vue
autonomous: true
requirements:
  - VENUE-03
  - VENUE-04

must_haves:
  truths:
    - "The search and filter bar has a gray-100 (#f3f4f6) background with no border and no shadow"
    - "The filter bar has no rounded corners — sharp corners consistent with flat design system"
    - "The selected venue count chip has a solid navy (#0d47a1) background with white bold text"
    - "The count chip has sharp corners — not a soft pill or rounded-lg shape"
    - "The count chip is hidden when 0 venues are selected (existing behavior retained)"
  artifacts:
    - path: "src/dialogs/wizard/WizardView.vue"
      provides: "Updated filter bar and count chip HTML + CSS"
      contains: "bg-blue-100"
  key_links:
    - from: "div.bg-blue-100 (line ~412)"
      to: "solid navy chip"
      via: "Tailwind class replacement"
      pattern: "selectedVenues.*selected.*length"
---

<objective>
Update the Step 1 filter bar and selected venue count chip in WizardView.vue. The filter bar switches from gray-50/rounded-lg to gray-100/no border-radius. The count chip switches from a soft blue-100 pill to a solid navy bold chip with white text.

Purpose: VENUE-03 and VENUE-04 — completes the flat design system rollout for Step 1 UI chrome.
Output: Modified WizardView.vue with updated filter bar container and count chip Tailwind classes, plus CSS for PrimeVue input overrides.
</objective>

<execution_context>
@/Users/justinhoward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinhoward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-venue-selection/04-01-SUMMARY.md

<interfaces>
<!-- From WizardView.vue template (lines 411-448, current) -->

Count chip (line ~412) — current:
```html
<div v-if="selectedVenues?.selected.length" class="bg-blue-100 dark:bg-blue-900 px-4 py-2 rounded-lg">
  <span class="text-blue-800 dark:text-blue-200 font-semibold">
    {{ selectedVenues.selected.length }} venue{{ selectedVenues.selected.length !== 1 ? 's' : '' }} selected
  </span>
</div>
```

Filter bar container (line ~429) — current:
```html
<div class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg mb-6">
  <div class="flex gap-4">
    <div class="relative flex-1">
      <i class="pi pi-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400" />
      <InputText v-model="filters['global'].value" placeholder="Search by name, type, or location..." class="w-full pl-10" />
    </div>
    <Select ... class="w-80" ... />
  </div>
</div>
```

<!-- PrimeVue deep override pattern (established in Phase 3): -->
<!-- ::v-deep() in <style scoped> for PrimeVue component internals -->
<!-- Existing overrides in the file use ::v-deep(.p-inputtext) etc -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update count chip and filter bar template classes</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
Make two targeted template changes in the Step 1 section.

**Change 1 — Count chip** (line ~412):
Replace the soft blue pill with a solid navy chip. Change:
```html
<div v-if="selectedVenues?.selected.length" class="bg-blue-100 dark:bg-blue-900 px-4 py-2 rounded-lg">
  <span class="text-blue-800 dark:text-blue-200 font-semibold">
    {{ selectedVenues.selected.length }} venue{{ selectedVenues.selected.length !== 1 ? 's' : '' }} selected
  </span>
</div>
```
To:
```html
<div v-if="selectedVenues?.selected.length" class="venue-count-chip">
  <span>{{ selectedVenues.selected.length }} venue{{ selectedVenues.selected.length !== 1 ? 's' : '' }} selected</span>
</div>
```
(Removes dark mode variants — flat design system is light-mode only per design decisions.)

**Change 2 — Filter bar container** (line ~429):
Replace `bg-gray-50 ... rounded-lg` with flat gray-100 no-radius styling. Change:
```html
<div class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg mb-6">
```
To:
```html
<div class="venue-filter-bar">
```
Keep all inner content (flex div, InputText, Select) unchanged.
  </action>
  <verify>
    <automated>grep -n "venue-count-chip\|venue-filter-bar" "/Users/justinhoward/Documents/Code/Blue Billboard/MapApp/src/dialogs/wizard/WizardView.vue"</automated>
  </verify>
  <done>Both `venue-count-chip` and `venue-filter-bar` classes appear in the template. The old `bg-blue-100` and `bg-gray-50 rounded-lg` classes are gone from those elements.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for count chip, filter bar, and flat PrimeVue input overrides</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
In the `<style scoped>` section, add the following CSS rules. Insert them after the existing venue card CSS block (after `.venue-card-city`) and before the `/* Info Form */` comment.

**Count chip** — solid navy, white bold text, sharp corners:
```css
/* Count Chip */
.venue-count-chip {
  display: inline-flex;
  align-items: center;
  background: #0d47a1;
  color: white;
  font-weight: 700;
  font-size: 0.9375rem;
  padding: 0.5rem 1rem;
  border-radius: 0;
}
```

**Filter bar container** — gray-100 background, no border, no shadow, no radius:
```css
/* Filter Bar */
.venue-filter-bar {
  background: #f3f4f6;
  padding: 1rem;
  margin-bottom: 1.5rem;
  border-radius: 0;
}
```

**PrimeVue InputText flat override** — gray-100 background, no border, no shadow on the search input inside the filter bar:
```css
.venue-filter-bar ::v-deep(.p-inputtext) {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 0;
  box-shadow: none !important;
}

.venue-filter-bar ::v-deep(.p-inputtext:focus) {
  border-color: #0d47a1;
  box-shadow: none !important;
}
```

**PrimeVue Select flat override** — no rounded corners, no shadow on the group filter dropdown:
```css
.venue-filter-bar ::v-deep(.p-select) {
  border-radius: 0;
  box-shadow: none !important;
}

.venue-filter-bar ::v-deep(.p-select:focus),
.venue-filter-bar ::v-deep(.p-select.p-focus) {
  box-shadow: none !important;
}
```
  </action>
  <verify>
    <automated>npm run build --prefix "/Users/justinhoward/Documents/Code/Blue Billboard/MapApp" 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. CSS rules for `venue-count-chip`, `venue-filter-bar`, and PrimeVue deep overrides are present in the style block. No TypeScript or Vite errors.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify:
1. `npm run build` passes with no errors
2. In browser dev server (`npm run dev`), open the wizard Step 1:
   - Filter bar background is gray-100 (#f3f4f6) with no visible border, shadow, or rounded corners
   - Search input and group filter dropdown inside the bar have no rounded corners and no shadow
   - Select at least one venue — the count chip appears with solid navy background (#0d47a1), white bold text, sharp corners (not pill-shaped)
   - Deselect all venues — count chip disappears (hidden when 0 selected)
</verification>

<success_criteria>
- Build passes without errors
- Filter bar: gray-100 background, no border, no shadow, no border-radius
- Count chip: solid navy (#0d47a1) background, white text, font-bold, border-radius: 0
- Count chip visible when venues selected, hidden when 0 selected
- No soft pill styling (rounded-lg) on either element
- PrimeVue inputs inside filter bar have no box-shadow in any state
</success_criteria>

<output>
After completion, create `.planning/phases/04-venue-selection/04-02-SUMMARY.md`
</output>
