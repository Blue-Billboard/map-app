---
phase: 06-quote-pricing
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/dialogs/wizard/WizardView.vue
autonomous: false
requirements:
  - QUOT-04
  - QUOT-05

must_haves:
  truths:
    - "The step header shows 'Your Custom Quote' with a 4px solid navy left-border accent"
    - "The CTA section is a solid gray-900 (#111827) background block — full-width, no gradient"
    - "CTA heading and description text are white on the gray-900 background"
    - "CTA email button is outlined (white border + white text, border-radius:0)"
    - "CTA phone button is solid white background with gray-900 text, border-radius:0"
    - "Two geometric circle decorators appear behind the pricing layout: one large (~450px) top-right, one medium (~280px) bottom-left"
    - "Circles are rgba(13,71,161,0.05) solid fill, absolutely positioned, pointer-events:none"
    - "The step container clips the circles via overflow:hidden without affecting card content"
  artifacts:
    - path: "src/dialogs/wizard/WizardView.vue"
      provides: "Step header with accent, CTA flat dark block, geometric circle divs"
      contains: "pricing-decoration"
  key_links:
    - from: ".pricing-step-header"
      to: "4px left-border accent"
      via: "border-left: 4px solid #0d47a1 on new wrapper element"
      pattern: "pricing-step-header"
    - from: ".cta-content"
      to: "gray-900 background"
      via: "background: #111827 replacing linear-gradient"
      pattern: "cta-content"
    - from: ".pricing-decoration"
      to: "circle decorators"
      via: "absolute positioned div elements inside step-container"
      pattern: "pricing-decoration"
---

<objective>
Add the step header navy accent, replace the CTA gradient block with a flat gray-900 section, redesign the CTA buttons for the dark background, and inject two geometric circle decorators as absolute-positioned background elements inside the Step 3 container.

Purpose: Deliver QUOT-04 (gray-900 CTA block) and QUOT-05 (geometric circle decoration). Also completes the step header editorial accent treatment.
Output: Template additions (circle divs, header restructure) and CSS changes in WizardView.vue.
</objective>

<execution_context>
@/Users/justinhoward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinhoward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quote-pricing/06-CONTEXT.md

@.planning/phases/06-quote-pricing/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Template structure from WizardView.vue — Step 3 section (lines 626-700) -->

Current Step 3 template structure:
```html
<div v-if="active === 2" key="step-2" class="step-container">
  <!-- Step header (lines 627-631): simple div with text-center -->
  <div class="mb-6 text-center">
    <h3 class="text-2xl font-bold text-gray-900">Your Custom Quote</h3>
    <p class="text-gray-600 mt-1 text-base">Choose the plan...</p>
    <p class="text-gray-500 text-sm mt-2">All prices shown are per month + VAT</p>
  </div>

  <!-- Pricing Cards (lines 634-675) -->
  <div class="pricing-grid"> ... </div>

  <!-- CTA Section (lines 678-693) -->
  <div class="cta-section">
    <div class="cta-content">
      <h4 class="cta-title">Ready to Get Started?</h4>
      <p class="cta-description">Contact us today...</p>
      <div class="cta-buttons">
        <a href="mailto:..." class="cta-button cta-button-primary">email</a>
        <a href="tel:..." class="cta-button cta-button-secondary">phone</a>
      </div>
    </div>
  </div>
</div>
```

Key CSS from plan 01 output (already in place):
  .step-container — existing, used by all steps. position: relative is likely not set — will need adding.

CTA section CSS currently (lines 1541-1608):
  .cta-section      — margin:2rem auto 1.5rem, max-width:700px
  .cta-content      — background: linear-gradient (being replaced), border-radius:16px, padding:2.5rem
  .cta-button       — border-radius:8px, padding:1rem 1.5rem
  .cta-button-primary   — background:white, color:#0d47a1
  .cta-button-secondary — background:rgba(255,255,255,0.2), border:2px solid white

Established flat design patterns:
  - border-radius: 0 everywhere
  - No translateY on hover
  - Navy #0d47a1 brand color
  - Light-mode only
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure step header and add geometric circle decorators to template</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
**Template changes inside the `v-if="active === 2"` step container:**

**1. Step header restructure (replace the existing `<div class="mb-6 text-center">` block):**

Replace:
```html
<div class="mb-6 text-center">
  <h3 class="text-2xl font-bold text-gray-900 dark:text-white">Your Custom Quote</h3>
  <p class="text-gray-600 dark:text-gray-300 mt-1 text-base">Choose the plan that works best for your business</p>
  <p class="text-gray-500 dark:text-gray-400 text-sm mt-2">All prices shown are per month + VAT</p>
</div>
```

With:
```html
<div class="pricing-step-header mb-6">
  <h3 class="text-2xl font-bold text-gray-900">Your Custom Quote</h3>
  <p class="text-gray-500 mt-1 text-base">Choose the plan that works best for your business</p>
  <p class="text-gray-400 text-sm mt-1">All prices shown are per month + VAT</p>
</div>
```

Note: Remove `dark:` Tailwind classes — flat design is light-mode only per established project decision.

**2. Add geometric circle decorators as the FIRST child inside the step container div:**

Add immediately after `<div v-if="active === 2" key="step-2" class="step-container">`:
```html
<!-- Geometric background decoration -->
<div class="pricing-decoration" aria-hidden="true">
  <div class="pricing-circle pricing-circle-large"></div>
  <div class="pricing-circle pricing-circle-medium"></div>
</div>
```

The `.pricing-decoration` wrapper and circles are positioned absolutely — they sit behind all content.

**CSS additions** (add after the existing `.step-container` CSS rule, or at end of step-related styles, before the `.pricing-grid` section):

```css
/* Pricing Step Header — editorial left accent */
.pricing-step-header {
  border-left: 4px solid #0d47a1;
  padding-left: 1rem;
}

/* Geometric background decoration */
.pricing-decoration {
  position: absolute;
  inset: 0;
  overflow: hidden;
  pointer-events: none;
  z-index: 0;
}

.pricing-circle {
  position: absolute;
  border-radius: 50%;
  background: rgba(13, 71, 161, 0.05);
}

.pricing-circle-large {
  width: 450px;
  height: 450px;
  top: -100px;
  right: -120px;
}

.pricing-circle-medium {
  width: 280px;
  height: 280px;
  bottom: -80px;
  left: -60px;
}
```

**Ensure `.step-container` has `position: relative`** — check the existing `.step-container` rule and add `position: relative` if not present. This anchors the absolute `.pricing-decoration` correctly.

Also ensure all pricing card content sits above the decoration — add `position: relative; z-index: 1` to `.pricing-grid` and `.cta-section` so they render above the circles.

Commit after this task: `feat(06-02): add step header accent and geometric circle decorators`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. The `.pricing-step-header` class exists in CSS with `border-left: 4px solid #0d47a1`. `.pricing-decoration` exists as an absolutely positioned element. `.pricing-circle-large` has `width: 450px`. `.step-container` has `position: relative`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace CTA gradient block with flat gray-900 section and redesign CTA buttons</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
**CSS changes to the CTA section:**

**1. `.cta-section` (line ~1541):**
- Keep `margin` and width but remove `max-width: 700px` constraint — the CTA should be full-width for a bold poster-style block
- Add `position: relative; z-index: 1` to ensure it sits above the circle decorators

**2. `.cta-content` (line ~1547):**
- Replace `background: linear-gradient(135deg, #0d47a1 0%, #1976d2 100%)` with `background: #111827`
- Change `border-radius: 16px` to `border-radius: 0`
- Keep `padding: 2.5rem`, `text-align: center`, `color: white`

**3. `.cta-button` (line ~1573):**
- Change `border-radius: 8px` to `border-radius: 0`
- Remove `transform: ...` from hover state if present — keep transition

**4. Replace `.cta-button-primary` (line ~1589):**
Current: white bg, `color: #0d47a1` (email button — per user decision this becomes outlined)

Replace with:
```css
.cta-button-primary {
  background: transparent;
  color: white;
  border: 2px solid white;
  border-radius: 0;
}

.cta-button-primary:hover {
  background: rgba(255, 255, 255, 0.1);
}
```

**5. Replace `.cta-button-secondary` (line ~1599):**
Current: `rgba(255,255,255,0.2)` bg, white text, white border (phone button — per user decision this becomes solid white)

Replace with:
```css
.cta-button-secondary {
  background: white;
  color: #111827;
  border: 2px solid white;
  border-radius: 0;
}

.cta-button-secondary:hover {
  background: #f3f4f6;
  border-color: #f3f4f6;
}
```

**6. Remove the CTA section dark mode entry** — locate `.cta-content` in the `@media (prefers-color-scheme: dark)` block and delete it (along with `.cta-button-primary` and `.cta-button-primary:hover` dark mode entries if they weren't already removed in plan 01).

Note: Per user decision, the CTA buttons are: email = outlined (white border + white text), phone = solid white (gray-900 text). This matches the CONTEXT.md decisions section exactly.

Commit after this task: `feat(06-02): replace CTA gradient with flat gray-900 block, redesign CTA buttons`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `.cta-content` has `background: #111827` and `border-radius: 0`. `.cta-button-primary` has `background: transparent; border: 2px solid white` (outlined email). `.cta-button-secondary` has `background: white; color: #111827` (solid white phone). No `linear-gradient` in any CTA CSS rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Step 3 header, circles, and CTA section visually</name>
  <action>Human verification step — confirm the step header accent, geometric circles, and flat gray-900 CTA block render correctly.</action>
  <what-built>Step header with navy editorial left-border accent, geometric circle background decoration behind the pricing layout, flat gray-900 CTA block, outlined email button (white border + white text), solid white phone button (gray-900 text)</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the wizard in a browser
2. Navigate to Step 3 (Quote & Pricing)
3. Verify the step header: "Your Custom Quote" has a visible 4px solid navy left-border accent, text is left-aligned (not centered)
4. Verify geometric circles: faint navy-tinted circles visible behind the pricing content — one large in the top-right area, one medium bottom-left, both bleeding off edges
5. Scroll to the CTA section at the bottom: verify it has a solid near-black (#111827) background — no gradient, no rounded corners
6. Verify email CTA button: transparent background, white border, white text ("sales@bluebillboard.co.uk")
7. Verify phone CTA button: solid white background, dark gray text ("0800 058 4275")
8. Both CTA buttons have flat corners (no border-radius)
9. Hover both CTA buttons: no vertical movement — just background tint change
10. Overall Step 3 impression: bold poster-style layout with the navy featured card dominating, decorative circles in the background, solid dark CTA at the bottom
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All changes verified via:
1. `npm run build` passes after each task
2. Visual checkpoint: step header has left-border accent, circles visible behind content, CTA is solid gray-900 block
3. No `linear-gradient` in `.cta-content` CSS
4. `.pricing-decoration` exists in template with two circle child divs
</verification>

<success_criteria>
1. `.pricing-step-header` has `border-left: 4px solid #0d47a1`
2. `.pricing-decoration` is absolutely positioned with `z-index: 0` and `pointer-events: none`
3. Two `.pricing-circle` elements exist with faint navy fill, bleeding off edges
4. `.cta-content` has `background: #111827` and `border-radius: 0`
5. `.cta-button-primary` is outlined: `transparent` bg + `border: 2px solid white`
6. `.cta-button-secondary` is solid white: `background: white; color: #111827`
7. No translateY on any CTA button hover
8. Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-quote-pricing/06-02-SUMMARY.md`
</output>
