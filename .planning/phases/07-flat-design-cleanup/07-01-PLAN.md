---
phase: 07-flat-design-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dialogs/wizard/WizardView.vue
autonomous: false
requirements:
  - WIZD-05
  - CAMP-01
  - CAMP-02
  - CAMP-03
  - QUOT-02

must_haves:
  truths:
    - "Step 2 info panel has no visible gradient — flat gray-50 (#f9fafb) surface only"
    - "Step 2 info panel has square corners — no border-radius"
    - "Step 3 pricing highlight rows have square corners — no border-radius"
    - "No linear-gradient remains in WizardView.vue scoped styles (excluding intentional decorators)"
    - "Only border-radius: 0 and border-radius: 50% (circle decorators) remain in scoped styles"
  artifacts:
    - path: "src/dialogs/wizard/WizardView.vue"
      provides: "Flat CSS for .info-section and .feature-item.highlight"
      contains: "background: #f9fafb"
  key_links:
    - from: ".info-section CSS rule"
      to: "Step 2 rendered surface"
      via: "scoped Vue style applied to .info-section wrapper element"
      pattern: "background: #f9fafb"
    - from: ".feature-item.highlight CSS rule"
      to: "Step 3 pricing card highlight rows"
      via: "scoped Vue style applied to .feature-item.highlight elements"
      pattern: "border-radius: 0"
---

<objective>
Close the two flat design gaps identified by the v1.1 milestone audit. Both violations are in `WizardView.vue`'s scoped style block — one gradient and two non-zero border-radius values that escaped Phases 4, 5, and 6.

Purpose: Complete the flat design system across all three wizard steps. CAMP-01, CAMP-02, CAMP-03, QUOT-02, and WIZD-05 are currently only partially satisfied because these two parent/internal rules were not caught in prior phases.

Output: `src/dialogs/wizard/WizardView.vue` with `.info-section` flat and `.feature-item.highlight` using `border-radius: 0`.
</objective>

<execution_context>
@/Users/justinhoward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinhoward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-flat-design-cleanup/07-RESEARCH.md

<interfaces>
<!-- Key CSS rules the executor must modify. Extracted from live source grep. -->
<!-- Use grep to locate by selector name — line numbers may drift. -->

From src/dialogs/wizard/WizardView.vue <style scoped> — CURRENT (violation) state:

```css
/* ~line 1110 — GAP 1 */
.info-section {
  margin-bottom: 2rem;
  padding: 1.5rem;
  background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);  /* VIOLATION */
  border-radius: 12px;                                               /* VIOLATION */
  border: 1px solid #e5e7eb;
  transition: all 0.3s ease;
}

/* ~line 1481 — GAP 2 */
.feature-item.highlight {
  padding: 0.5rem;
  border-radius: 6px;   /* VIOLATION */
  font-weight: 600;
  border-left: 3px solid transparent;
  font-size: 0.8rem;
}
```

Target (flat) state after this plan:

```css
.info-section {
  margin-bottom: 2rem;
  padding: 1.5rem;
  background: #f9fafb;   /* was: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%) */
  border-radius: 0;       /* was: 12px */
  border: 1px solid #e5e7eb;
  transition: all 0.3s ease;
}

.feature-item.highlight {
  padding: 0.5rem;
  border-radius: 0;      /* was: 6px */
  font-weight: 600;
  border-left: 3px solid transparent;
  font-size: 0.8rem;
}
```

DO NOT TOUCH:
- `.info-section:hover` block — only changes border-color, not gradient or radius
- `@media (prefers-color-scheme: dark)` block that overrides `.info-section background` — inherits border-radius: 0 from base rule automatically; no edit needed
- `.save-quote-section` and `.save-quote-form` — dead CSS, out of scope
- `border-radius: 50%` on `.pricing-circle` decorators — intentional circles, not a violation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten .info-section — remove gradient and round corners</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
    In the `<style scoped>` block of WizardView.vue, locate the `.info-section { }` rule (search for `.info-section {` — do NOT use line numbers as they may have drifted).

    Make exactly two property changes within the existing rule block:
    1. Replace `background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%)` with `background: #f9fafb`
    2. Replace `border-radius: 12px` with `border-radius: 0`

    All other properties in the rule (`margin-bottom`, `padding`, `border`, `transition`) must remain unchanged.

    Do NOT edit:
    - The `.info-section:hover` block below it (only changes border-color — correct as-is)
    - The `@media (prefers-color-scheme: dark)` block that contains `.info-section` — after the base rule sets `border-radius: 0`, the dark mode override inherits it automatically
    - Any `::v-deep` blocks scoped under `.info-section`
  </action>
  <verify>
    <automated>grep -n "linear-gradient" "/Users/justinhoward/Documents/Code/Blue Billboard/MapApp/src/dialogs/wizard/WizardView.vue" && echo "FAIL: gradient still present" || echo "PASS: no gradient found"</automated>
  </verify>
  <done>`.info-section` has `background: #f9fafb` and `border-radius: 0`. `grep -n "linear-gradient"` returns zero results. `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten .feature-item.highlight — remove border-radius</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
    In the `<style scoped>` block of WizardView.vue, locate the `.feature-item.highlight { }` rule (search for `.feature-item.highlight {`).

    Make exactly one property change within the existing rule block:
    1. Replace `border-radius: 6px` with `border-radius: 0`

    All other properties (`padding`, `font-weight`, `border-left`, `font-size`) must remain unchanged.

    Note: The tier-specific overrides (`.pricing-card.starter .feature-item.highlight`, `.pricing-card.optimal .feature-item.highlight`, etc.) do not set border-radius — they only set border-left color. Do not touch them.

    After both tasks are complete, run the full build to confirm clean compile:
    `npm run build`

    Then run the combined grep verification:
    `grep -n "linear-gradient\|border-radius: 12px\|border-radius: 6px" src/dialogs/wizard/WizardView.vue`
    This must return zero results.
  </action>
  <verify>
    <automated>cd "/Users/justinhoward/Documents/Code/Blue Billboard/MapApp" && grep -n "linear-gradient\|border-radius: 12px\|border-radius: 6px" src/dialogs/wizard/WizardView.vue && echo "FAIL: violations remain" || echo "PASS: all violations cleared"; npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>`.feature-item.highlight` has `border-radius: 0`. Combined grep returns zero results for `linear-gradient`, `border-radius: 12px`, `border-radius: 6px`. `npm run build` exits 0.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual sign-off — Step 2 and Step 3 flat rendering</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>Run `npm run dev`, open the wizard, and visually verify Step 2 and Step 3 changes described below before marking complete.</action>
  <what-built>Two targeted CSS property changes in WizardView.vue:
    1. .info-section: gradient removed (now solid #f9fafb), border-radius removed (now 0)
    2. .feature-item.highlight: border-radius removed (now 0)</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app
    2. Open the quote wizard (click any venue pin, open wizard)
    3. Navigate to Step 2 (Campaign Details)
       - Confirm the info panel (containing Duration, Discounts, Selected Venues sections) has a flat light-gray surface with NO visible gradient transition
       - Confirm the info panel has sharp square corners (no rounding)
    4. Navigate to Step 3 (Quote & Pricing)
       - Confirm the highlighted feature rows inside each pricing card have sharp square corners (no rounding)
       - Confirm the featured navy card still looks correct (its highlight rows should also be sharp-cornered)
    5. All other wizard elements should appear unchanged
  </how-to-verify>
  <verify>
    <automated>cd "/Users/justinhoward/Documents/Code/Blue Billboard/MapApp" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed flat rendering in Step 2 and Step 3. Build passes.</done>
  <resume-signal>Type "approved" to complete the phase, or describe any visual issues seen</resume-signal>
</task>

</tasks>

<verification>
**Automated checks (run after all tasks):**

```bash
# 1. No flat design violations in wizard styles
grep -n "linear-gradient\|border-radius: 12px\|border-radius: 6px" src/dialogs/wizard/WizardView.vue
# Expected: zero results

# 2. Only valid border-radius values remain (0 and 50% for circles)
grep -n "border-radius" src/dialogs/wizard/WizardView.vue
# Expected: only lines containing "border-radius: 0" or "border-radius: 50%"

# 3. Clean TypeScript build
npm run build
# Expected: exit 0
```

**Manual visual check:**
- Step 2 info panel: flat gray surface, square corners
- Step 3 pricing cards: highlight rows have square corners
</verification>

<success_criteria>
1. `grep -n "linear-gradient" src/dialogs/wizard/WizardView.vue` returns zero results
2. `grep -n "border-radius: 12px" src/dialogs/wizard/WizardView.vue` returns zero results
3. `grep -n "border-radius: 6px" src/dialogs/wizard/WizardView.vue` returns zero results
4. `npm run build` exits 0
5. Visual confirmation: Step 2 info panel renders flat (no gradient, square corners)
6. Visual confirmation: Step 3 pricing highlight rows render with square corners
</success_criteria>

<output>
After completion, create `.planning/phases/07-flat-design-cleanup/07-01-SUMMARY.md` using the summary template.
</output>
