---
phase: 06-quote-pricing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dialogs/wizard/WizardView.vue
autonomous: false
requirements:
  - QUOT-01
  - QUOT-02
  - QUOT-03

must_haves:
  truths:
    - "The Most Popular (featured) card renders as a solid navy (#0d47a1) background block with all text, icons, and feature items in white"
    - "All four pricing cards have border-radius:0 and no box-shadow"
    - "The Starter card has a white background with gray-200 border"
    - "The Optimal card has a #f8faff tint background with solid navy 1px border"
    - "The Premium card has a #eef2ff tint background with solid navy 2px border"
    - "Price amounts display at 3rem (text-5xl), font-bold, line-height:1"
    - "The 'Most Popular' badge is a white block with navy text and border-radius:0"
    - "The 'Best Value' badge is a solid navy block with white text and border-radius:0"
    - "Non-featured cards scale to scale(1.02) on hover — no translateY, no box-shadow"
    - "Featured card has no hover transform"
    - "All pricing card dark mode entries are removed"
  artifacts:
    - path: "src/dialogs/wizard/WizardView.vue"
      provides: "Flat pricing card CSS for all 4 tiers"
      contains: "background: #0d47a1"
  key_links:
    - from: ".pricing-card.featured"
      to: "solid navy block"
      via: "background: #0d47a1 replacing linear-gradient"
      pattern: "pricing-card\\.featured"
    - from: ".amount"
      to: "3rem font size"
      via: "font-size: 3rem replacing per-tier overrides"
      pattern: "\\.amount"
---

<objective>
Overhaul all pricing card CSS in Step 3 — replace every gradient, shadow, and border-radius violation with flat solid-color block styling. The featured card becomes a solid navy poster block. All price amounts scale up to text-5xl. Tier badges become flat solid blocks. All dark mode pricing entries are removed.

Purpose: Deliver QUOT-01 (featured navy block), QUOT-02 (flat color-block cards), QUOT-03 (large price display).
Output: Updated CSS-only changes in WizardView.vue — no template changes in this plan.
</objective>

<execution_context>
@/Users/justinhoward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinhoward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quote-pricing/06-CONTEXT.md

@.planning/phases/05-campaign-details-mobile/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Existing CSS class structure in WizardView.vue relevant to this plan. -->
<!-- All CSS lives in the <style scoped> block starting at line 711. -->

Pricing card classes (lines 1237-1294):
  .pricing-card          — base: white bg, border:2px #e5e7eb, border-radius:12px, padding:1.25rem
  .pricing-card.starter  — opacity:0.95, hover: translateY(-2px)
  .pricing-card.optimal  — transform:scale(1.01), hover: translateY(-2px)
  .pricing-card.featured — transform:scale(1.03), linear-gradient bg, hover: translateY(-3px)
  .pricing-card.premium  — transform:scale(1.04), linear-gradient bg, hover: translateY(-4px)

Tier badge classes (lines 1296-1319):
  .tier-badge            — position:absolute, top:-12px, border-radius:20px, padding:0.375rem 1.25rem
  .tier-badge-2          — linear-gradient(135deg, #0d47a1, #1976d2), color:white  [Most Popular]
  .tier-badge-3          — linear-gradient(135deg, #0d47a1, #1976d2), color:white  [Best Value]

Pricing name gradient classes (lines 1338-1352):
  .pricing-card.featured .pricing-name  — background-clip gradient text trick
  .pricing-card.premium .pricing-name   — background-clip gradient text trick

Price container (lines 1362-1378):
  .pricing-price-container              — background:rgba(13,71,161,0.02), border-radius:8px
  .pricing-card.featured/premium .pricing-price-container — linear-gradient bg + border

Amount (lines 1393-1411):
  .amount                               — font-size:2.25rem, color:#0d47a1
  .pricing-card.optimal .amount         — font-size:2.5rem
  .pricing-card.featured .amount        — font-size:2.75rem
  .pricing-card.premium .amount         — font-size:3rem

Feature highlight (lines 1456-1484):
  .pricing-card.featured .feature-item.highlight — linear-gradient bg
  .pricing-card.premium .feature-item.highlight  — linear-gradient bg

CTA buttons (lines 1487-1538):
  .pricing-cta-button          — navy bg, white text, border:2px solid navy
  .pricing-cta-button:hover    — hover: translateY(-2px)
  .pricing-cta-button.featured-cta  — !important navy (redundant after flat overhaul)
  .pricing-cta-button.premium-cta   — linear-gradient bg !important

Dark mode pricing block (lines 1686-1843):
  Lines 1686-1843: all pricing card, pricing name, amount, price-container, feature-highlight,
  pricing-cta-button, featured-cta, premium-cta, cta-content, cta-button-primary entries

Established flat design patterns (from Phases 3-5):
  - border-radius: 0 (all components)
  - box-shadow: none (all components)
  - hover: background intensification + scale(1.02), NO translateY
  - Light-mode only — remove dark mode sections
  - Navy brand color: #0d47a1
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flatten pricing card base, tier variants, and tier badges</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
Apply the following CSS changes in the `<style scoped>` block of WizardView.vue:

**1. `.pricing-card` base (line ~1238):**
- Change `border-radius: 12px` to `border-radius: 0`
- Remove or set `box-shadow: none` (ensure none exists)
- Keep the rest (white bg, 2px border, padding, transition, flex column)

**2. `.pricing-card.starter` (line ~1251):**
- Remove `opacity: 0.95`
- Background: `white`, border: `1px solid #e5e7eb` (explicitly override base if needed)
- Hover: `transform: scale(1.02)` only — remove `translateY(-2px)`, no border-color change needed

**3. `.pricing-card.optimal` (line ~1261):**
- Remove `transform: scale(1.01)` from base state — cards start at scale(1) in flat design
- Background: `#f8faff`, border: `1px solid #0d47a1`
- Hover: `transform: scale(1.02)` only — remove the `scale(1.01) translateY(-2px)` compound

**4. `.pricing-card.featured` (line ~1271):**
- Remove `transform: scale(1.03)` from base state
- Replace `linear-gradient` background with `background: #0d47a1` (solid navy)
- Set `border: none` (card IS the background color — no border needed)
- Hover rule: remove entirely (no hover effect on featured card per user decision)

**5. `.pricing-card.premium` (line ~1284):**
- Remove `transform: scale(1.04)` from base state
- Replace `linear-gradient` background with `background: #eef2ff`
- Keep `border: 2px solid #0d47a1`
- Hover: `transform: scale(1.02)` only — remove `translateY(-4px)` compound

**6. `.tier-badge` (line ~1296):**
- Change `border-radius: 20px` to `border-radius: 0`
- Keep positioning (position:absolute, top:-12px, translateX(-50%)) and font styles

**7. `.tier-badge-2` — Most Popular (line ~1311):**
- Replace `linear-gradient(135deg, #0d47a1 0%, #1976d2 100%)` with `background: white`
- Set `color: #0d47a1`

**8. `.tier-badge-3` — Best Value (line ~1316):**
- Replace `linear-gradient(135deg, #0d47a1 0%, #1976d2 100%)` with `background: #0d47a1` (solid navy)
- Keep `color: white`

Commit after this task: `feat(06-01): flatten pricing card base, tier variants, and tier badges`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `.pricing-card` has `border-radius: 0`. `.pricing-card.featured` has `background: #0d47a1` (no linear-gradient). `.tier-badge-2` has `background: white` and `color: #0d47a1`. `.tier-badge` has `border-radius: 0`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flatten pricing names, price display, feature highlights, CTA buttons, and remove dark mode</name>
  <files>src/dialogs/wizard/WizardView.vue</files>
  <action>
Continue CSS changes in WizardView.vue:

**1. Pricing name gradient removal:**
- Delete `.pricing-card.featured .pricing-name` gradient text rule (the `background: linear-gradient`, `-webkit-background-clip: text`, `-webkit-text-fill-color: transparent`, `background-clip: text` block at lines ~1338-1344)
- Replace with: `.pricing-card.featured .pricing-name { color: white; font-size: 1.625rem; }`
- Delete `.pricing-card.premium .pricing-name` gradient text rule (lines ~1346-1352)
- Replace with: `.pricing-card.premium .pricing-name { color: #0d47a1; font-size: 1.75rem; }`

**2. `.pricing-price-container` (line ~1362):**
- Remove the sub-box styling: set `background: transparent`, remove `border-radius: 8px`, remove `padding: 0.875rem` (or reduce to 0), keep `margin-bottom: 1rem` and `text-align: center`
- Delete the variant rules `.pricing-card.optimal .pricing-price-container` and `.pricing-card.featured .pricing-price-container, .pricing-card.premium .pricing-price-container` entirely — the container should be transparent across all tiers

**3. Price amount unification (lines ~1393-1411):**
- Change base `.amount` font-size from `2.25rem` to `3rem` (text-5xl equivalent)
- Change `.amount` color from `#0d47a1` to `#111827` (dark gray — featured card will override to white)
- Delete the per-tier `.pricing-card.optimal .amount`, `.pricing-card.featured .amount`, `.pricing-card.premium .amount` overrides — all tiers now use the same 3rem size
- Add: `.pricing-card.featured .amount { color: white; }` (featured-only color override)
- Add: `.pricing-card.featured .currency { color: white; }` and `.pricing-card.featured .pricing-description { color: rgba(255,255,255,0.85); }`

**4. Feature item colors on featured card:**
- Change `.pricing-card.featured .feature-item i { color: white; }` (currently `#0d47a1` — must be white on navy bg)
- Add `.pricing-card.featured .feature-item { color: white; }`

**5. Feature highlight backgrounds:**
- Replace `.pricing-card.featured .feature-item.highlight` linear-gradient with `background: rgba(255,255,255,0.15); border-left-color: white;`
- Replace `.pricing-card.premium .feature-item.highlight` linear-gradient with `background: #dde8ff; border-left-color: #0d47a1;`

**6. `.pricing-cta-button` base hover (line ~1502):**
- Remove `transform: translateY(-2px)` from `.pricing-cta-button:hover` — flat design, no translateY
- Add `transform: none` if needed to override any inherited transform

**7. Replace `.pricing-cta-button.featured-cta` (lines ~1509-1522):**
- Delete the existing featured-cta rules
- Replace with:
  ```css
  .pricing-cta-button.featured-cta,
  .p-button.pricing-cta-button.featured-cta:not(.p-button-outlined):not(.p-button-text) {
    background: white !important;
    color: #0d47a1 !important;
    border: 2px solid white !important;
    font-weight: 700;
    border-radius: 0;
  }
  .pricing-cta-button.featured-cta:hover,
  .p-button.pricing-cta-button.featured-cta:hover:not(.p-button-outlined):not(.p-button-text) {
    background: #f0f4ff !important;
    border-color: #f0f4ff !important;
  }
  ```

**8. Delete `.pricing-cta-button.premium-cta` gradient rules (lines ~1525-1538):**
- Delete both the base and hover `.premium-cta` rules entirely
- Premium card uses the standard navy button (`.pricing-cta-button` base is already navy — no special override needed)

**9. Remove all pricing-related dark mode entries (lines ~1686-1843):**
- Delete the entire block from `/* Pricing Cards Dark Mode */` comment through the end of the CTA section dark mode entries (`.cta-content`, `.cta-button-primary`, `.cta-button-primary:hover`)
- Stop before the `/* Pricing CTA Buttons Dark Mode */` section header — remove that block too
- Keep non-pricing dark mode entries above the pricing block (`.venue-card`, `.info-section`, `.save-quote-section`, etc.) — only remove pricing-specific entries

Commit after this task: `feat(06-01): flatten price display, feature items, CTA buttons, remove dark mode pricing`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `.amount` has `font-size: 3rem`. `.pricing-card.featured .pricing-name` uses `color: white` (not gradient). `.pricing-cta-button.featured-cta` has `background: white` and `color: #0d47a1`. No `linear-gradient` remains inside any pricing card CSS rule (search confirms). No pricing card dark mode entries remain in the `@media (prefers-color-scheme: dark)` block.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Step 3 flat pricing card styling visually</name>
  <action>Human verification step — confirm the flat pricing card styling applied by Tasks 1 and 2 is correct visually.</action>
  <what-built>Flat pricing card CSS overhaul — solid navy featured block, flat tier variants, unified text-5xl price display, white inverted featured CTA button, flat tier badges, and all dark mode pricing entries removed</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the wizard in a browser
2. Navigate to Step 3 (Quote & Pricing)
3. Verify the Most Popular card: solid navy background, ALL text/icons/features in white, white badge with navy text in top position
4. Verify Starter card: plain white background, gray-200 border, flat corners (no border-radius)
5. Verify Optimal card: faint blue-tint (#f8faff) background, solid navy border, flat corners
6. Verify Premium card: #eef2ff blue tint background, solid navy border, flat corners, Best Value badge in solid navy with white text
7. Verify price amounts: all four cards display the pound amount at the same large size (3rem), bold, tight leading — no per-card scaling differences
8. Hover Starter, Optimal, Premium: they scale up slightly (scale 1.02) — no vertical movement, no shadow
9. Hover Featured card: NO hover effect — it stays static
10. Featured card "Get Started" button: white background with navy text
11. All other cards "Get Started" buttons: navy background with white text
12. Enable browser dark mode: pricing cards should remain unchanged (no dark mode overrides)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All CSS changes verified via:
1. `npm run build` passes without errors after each task
2. Visual checkpoint: Featured card is solid navy poster block, all four cards are flat with no gradients/shadows/border-radius, prices at 3rem
3. No `linear-gradient` in any pricing card CSS rule (grep confirms)
4. Dark mode entries for pricing removed
</verification>

<success_criteria>
1. `.pricing-card.featured` has `background: #0d47a1` — solid navy, all white text
2. All pricing cards have `border-radius: 0` and `box-shadow: none`
3. `.amount` has `font-size: 3rem` with `line-height: 1`
4. `.tier-badge-2` (Most Popular) has `background: white; color: #0d47a1`
5. `.tier-badge-3` (Best Value) has `background: #0d47a1; color: white`
6. No `linear-gradient` exists inside pricing card CSS rules
7. Featured card CTA button: `background: white; color: #0d47a1`
8. Dark mode pricing entries removed
</success_criteria>

<output>
After completion, create `.planning/phases/06-quote-pricing/06-01-SUMMARY.md`
</output>
